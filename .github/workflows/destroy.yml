# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: destroy
'on':
  workflow_dispatch: {}
jobs:
  destroy:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_secret_key: ${{ secrets.DJANGO_SECRET_KEY }}
      TF_VAR_domain_name: url-shortener-app.udap.dev
    steps:
    - uses: actions/checkout@v4
    - uses: hashicorp/setup-terraform@v3
    - name: Terraform destroy
      working-directory: infra
      run: 'terraform init -input=false -reconfigure -backend-config="bucket=${{ secrets.TF_STATE_BUCKET
        }}" -backend-config="key=${{ secrets.PROJECT_NAME }}/terraform.tfstate" -backend-config="region=${AWS_REGION}"

        terraform destroy -input=false -auto-approve'
